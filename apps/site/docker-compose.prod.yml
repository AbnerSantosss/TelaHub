# Stack NOVA do site de vendas, em paralelo à antiga.
#
# Arquivo separado do `docker-compose.yml`, que é o usado pela stack antiga.
# Porta própria (4038), diferente da 42938 da stack antiga: o túnel desta VPS
# roda no HOST e roteia para `localhost:PORTA`, então publicar é obrigatório, e
# repetir a porta da stack antiga impediria o container de subir.

services:
  site:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        # Lidos em tempo de BUILD pelo Vite — defini-los como variável de
        # ambiente da stack não teria efeito nenhum sobre o bundle.
        - VITE_CHECKOUT_URL=${VITE_CHECKOUT_URL:-https://checkout.proxserverabner.site}
        - VITE_APP_URL=${VITE_APP_URL:-https://painel.proxserverabner.site}
    container_name: telahub-site-prod
    restart: unless-stopped
    ports:
      # vendas.proxserverabner.site -> localhost:4038
      - "4038:80"
    networks:
      - telahub_net
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:80/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

networks:
  telahub_net:
    external: true
